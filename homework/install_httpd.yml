- name: Ansible Playbook to Install and Setup Apache on Ubuntu
  hosts: ubuntu-task
  become: yes
  tasks:
    - name: Install latest version of 
      apt: name=apache2 update_cache=yes state=latest
   
    - name: Make sure a service is running
      systemd:
        state: started
        name: apache2
        enabled: yes
